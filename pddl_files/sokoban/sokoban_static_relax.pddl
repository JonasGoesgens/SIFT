(define (domain sokoban-domain)
  (:requirements :equality :negative-preconditions)
  (:predicates (has_player ?x)
               (has_box ?x)
               (free ?x)
               (adjacent ?x ?y)
               (adjacent_2 ?x ?y))
  (:action move-player
           :parameters (?x ?y)
           :precondition (and (free ?y)
                              (has_player ?x))
           :effect		   (and (has_player ?y)
                              (not (has_player ?x))))
  (:action push-box
		   :parameters (?x ?y ?z)
		   :precondition (and (has_player ?x)
                          (has_box ?y)
                          (free ?z))
	       :effect (and (has_box ?z)
                        (free ?y)
						(not(has_box ?y))
                        (not(free ?z))
						(has_player ?y)
						(not(has_player ?x))))
  )
